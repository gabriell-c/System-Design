[project]
name = "archia-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn",
  "sqlalchemy",
  "aiosqlite",
  "pydantic-settings",
  "passlib[bcrypt]",
  "python-jose",
  "httpx",
  "asyncpg",
  "redis",
  "celery",
  "python-multipart",
  "pyjwt",
  "boto3",
  "sentry-sdk",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
